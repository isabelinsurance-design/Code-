{
  "name": "samia-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SAMIA — Asistente del equipo de Medicare de Isabel Fuentes. Backend (Fase 1: Fundacion).",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file=.env server/index.js",
    "start:noenv": "node server/index.js",
    "dev": "node --watch --env-file=.env server/index.js"
  }
}
