# Pin Node 22 para el build de Nixpacks (Railway). El backend es zero-dep, asi
# que no hay fase de install que dependa de paquetes; solo necesita el runtime.
[phases.setup]
nixPkgs = ["nodejs_22"]

[start]
cmd = "npm start"
